INSERT INTO calendar_entries (user_id, calendar_id, journey_id, journey_day, source_type, content, emotion_id, emotion_name, color)
SELECT 
  auth.uid(),
  (SELECT id FROM mood_calendar WHERE user_id = auth.uid() AND entry_date = CURRENT_DATE),
  'wewnetrzne-dziecko',
  1,
  'journey',
  'Moja odpowiedź na pytanie...',
  'joy',
  'Radość',
  '#FFD93D';

  INSERT INTO mood_calendar (user_id, entry_date, primary_emotion_id, primary_emotion_name, color)
VALUES (auth.uid(), CURRENT_DATE, 'joy', 'Radość', '#FFD93D')
ON CONFLICT (user_id, entry_date) DO UPDATE
SET primary_emotion_id = EXCLUDED.primary_emotion_id,
    primary_emotion_name = EXCLUDED.primary_emotion_name,
    color = EXCLUDED.color
RETURNING id;

INSERT INTO emotions (id, name, name_en, spectrum, parent_id, wheel_position, is_active)
VALUES
('ecstasy', 'Ekstaza', 'Ecstasy', 'positive', 'joy', 0, TRUE),
('love', 'Miłość', 'Love', 'positive', 'joy', 45, TRUE),
('admiration', 'Podziw', 'Admiration', 'positive', 'trust', 45, TRUE),
('acceptance', 'Akceptacja', 'Acceptance', 'positive', 'trust', NULL, TRUE),
('apprehension', 'Niepokój', 'Apprehension', 'negative', 'fear', NULL, TRUE),
('terror', 'Terror', 'Terror', 'negative', 'fear', 90, TRUE),
('distraction', 'Rozproszenie', 'Distraction', 'neutral', 'surprise', NULL, TRUE),
('amazement', 'Zadziwienie', 'Amazement', 'neutral', 'surprise', 135, TRUE),
('pensiveness', 'Zaduma', 'Pensiveness', 'negative', 'sadness', NULL, TRUE),
('grief', 'Żal', 'Grief', 'negative', 'sadness', 180, TRUE),
('boredom', 'Nuda', 'Boredom', 'negative', 'disgust', NULL, TRUE),
('loathing', 'Nienawiść', 'Loathing', 'negative', 'disgust', 225, TRUE),
('annoyance', 'Irytacja', 'Annoyance', 'negative', 'anger', NULL, TRUE),
('rage', 'Furia', 'Rage', 'negative', 'anger', 270, TRUE),
('interest', 'Zainteresowanie', 'Interest', 'positive', 'anticipation', NULL, TRUE),
('vigilance', 'Czujność', 'Vigilance', 'neutral', 'anticipation', 315, TRUE);

INSERT INTO emotions (id, name, name_en, spectrum, parent_id, wheel_position, is_active)
VALUES 
('joy', 'Radość', 'Joy', 'positive', NULL, NULL, TRUE),
('sadness', 'Smutek', 'Sadness', 'negative', NULL, NULL, TRUE),
('fear', 'Strach', 'Fear', 'negative', NULL, NULL, TRUE),
('anger', 'Złość', 'Anger', 'negative', NULL, NULL, TRUE),
('trust', 'Zaufanie', 'Trust', 'positive', NULL, NULL, TRUE),
('surprise', 'Zaskoczenie', 'Surprise', 'neutral', NULL, NULL, TRUE),
('disgust', 'Obrzydzenie', 'Disgust', 'negative', NULL, NULL, TRUE),
('anticipation', 'Antcypacja', 'Anticipation', 'positive', NULL, NULL, TRUE);


ALTER TABLE user_settings ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Own settings select"
ON user_settings
FOR SELECT
USING (user_id = auth.uid());

CREATE POLICY "Own settings upsert"
ON user_settings
FOR INSERT
WITH CHECK (user_id = auth.uid());

CREATE POLICY "Own settings update"
ON user_settings
FOR UPDATE
USING (user_id = auth.uid())
WITH CHECK (user_id = auth.uid());

ALTER TABLE calendar_entries ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Own entries select"
ON calendar_entries
FOR SELECT
USING (user_id = auth.uid());

CREATE POLICY "Own entries insert"
ON calendar_entries
FOR INSERT
WITH CHECK (user_id = auth.uid());

CREATE POLICY "Own entries delete"
ON calendar_entries
FOR DELETE
USING (user_id = auth.uid());

ALTER TABLE mood_calendar ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Own calendar select"
ON mood_calendar
FOR SELECT
USING (user_id = auth.uid());

CREATE POLICY "Own calendar insert"
ON mood_calendar
FOR INSERT
WITH CHECK (user_id = auth.uid());

CREATE POLICY "Own calendar update"
ON mood_calendar
FOR UPDATE
USING (user_id = auth.uid())
WITH CHECK (user_id = auth.uid());

ALTER TABLE user_mood_checkins ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Own checkins select"
ON user_mood_checkins
FOR SELECT
USING (user_id = auth.uid());

CREATE POLICY "Own checkins insert"
ON user_mood_checkins
FOR INSERT
WITH CHECK (user_id = auth.uid());

ALTER TABLE emotions ENABLE ROW LEVEL SECURITY;
ALTER TABLE emotion_questions ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Emotions public"
ON emotions
FOR SELECT
USING (true);

CREATE POLICY "Emotion questions public"
ON emotion_questions
FOR SELECT
USING (true);

ALTER TABLE user_responses ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Own responses select"
ON user_responses
FOR SELECT
USING (user_id = auth.uid());

CREATE POLICY "Own responses insert"
ON user_responses
FOR INSERT
WITH CHECK (user_id = auth.uid());

ALTER TABLE user_journey_progress ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Own progress select"
ON user_journey_progress
FOR SELECT
USING (user_id = auth.uid());

CREATE POLICY "Own progress insert"
ON user_journey_progress
FOR INSERT
WITH CHECK (user_id = auth.uid());

CREATE POLICY "Own progress update"
ON user_journey_progress
FOR UPDATE
USING (user_id = auth.uid())
WITH CHECK (user_id = auth.uid());

ALTER TABLE journeys ENABLE ROW LEVEL SECURITY;
ALTER TABLE journey_days ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Journeys are public"
ON journeys
FOR SELECT
USING (true);

CREATE POLICY "Journey days are public"
ON journey_days
FOR SELECT
USING (true);

CREATE POLICY "Users can delete their own mood entries"
ON mood_calendar
FOR DELETE
USING (user_id = auth.uid());

CREATE POLICY "Users can update their own mood entries"
ON mood_calendar
FOR UPDATE
USING (user_id = auth.uid())
WITH CHECK (user_id = auth.uid());

CREATE POLICY "Users can insert their own mood entries"
ON mood_calendar
FOR INSERT
WITH CHECK (user_id = auth.uid());

CREATE POLICY "Users can view their own mood entries"
ON mood_calendar
FOR SELECT
USING (user_id = auth.uid());

ALTER TABLE mood_calendar ENABLE ROW LEVEL SECURITY;

INSERT INTO calendar_entries (user_id, calendar_id, journey_id, journey_day, source_type, content, emotion_id, emotion_name, color)
SELECT 
  'user-uuid',
  (SELECT id FROM mood_calendar WHERE user_id = 'user-uuid' AND entry_date = CURRENT_DATE),
  'wewnetrzne-dziecko',
  1,
  'journey',
  'Moja odpowiedź na pytanie...',
  'joy',
  'Radość',
  '#FFD93D';

  SELECT 
  mc.id,
  mc.entry_date,
  mc.primary_emotion_id,
  mc.primary_emotion_name,
  mc.color as primary_color,

  COALESCE(
    json_agg(
      json_build_object(
        'id', ce.id,
        'content', ce.content,
        'emotion_name', ce.emotion_name,
        'color', ce.color,
        'source_type', ce.source_type
      )
    ) FILTER (WHERE ce.id IS NOT NULL),
    '[]'
  ) as entries

FROM mood_calendar mc
LEFT JOIN calendar_entries ce 
  ON mc.id = ce.calendar_id

WHERE mc.user_id = auth.uid()
  AND mc.entry_date BETWEEN '2024-01-01' AND '2024-01-31'

GROUP BY 
  mc.id, 
  mc.entry_date,
  mc.primary_emotion_id,
  mc.primary_emotion_name,
  mc.color

ORDER BY mc.entry_date;

SELECT
  (SELECT COUNT(DISTINCT journey_id)
   FROM user_journey_progress
   WHERE user_id = auth.uid()) AS journeys_started,

  (SELECT COUNT(*)
   FROM user_journey_progress
   WHERE user_id = auth.uid()
     AND status = 'completed') AS journeys_completed,

  (SELECT COUNT(*)
   FROM user_responses
   WHERE user_id = auth.uid()) AS total_responses,

  (SELECT COUNT(DISTINCT DATE(checked_in_at))
   FROM user_mood_checkins
   WHERE user_id = auth.uid()) AS mood_days_logged;

 SELECT jd.question, jd.question_en, jd.day_name
FROM journey_days jd
JOIN user_journey_progress ujp 
  ON jd.journey_id = ujp.journey_id
WHERE ujp.user_id = auth.uid()
  AND ujp.status = 'in_progress'
  AND jd.day_number = ujp.current_day;
  

  SELECT j.*, ujp.current_day, ujp.status
FROM journeys j
JOIN user_journey_progress ujp ON j.id = ujp.journey_id
WHERE ujp.user_id = auth.uid()
  AND ujp.status = 'in_progress'
ORDER BY ujp.started_at DESC
LIMIT 1;

CREATE INDEX idx_user_journey_progress_user ON user_journey_progress(user_id);
CREATE INDEX idx_user_journey_progress_status ON user_journey_progress(status);

-- Odpowiedzi użytkownika
CREATE INDEX idx_user_responses_user ON user_responses(user_id);
CREATE INDEX idx_user_responses_created ON user_responses(created_at);

-- Mood check-ins
CREATE INDEX idx_user_mood_checkins_user ON user_mood_checkins(user_id);
CREATE INDEX idx_user_mood_checkins_date ON user_mood_checkins(checked_in_at);

-- Kalendarz nastrojów
CREATE INDEX idx_mood_calendar_user_date ON mood_calendar(user_id, entry_date);

-- Wpisy kalendarza
CREATE INDEX idx_calendar_entries_calendar ON calendar_entries(calendar_id);
CREATE INDEX idx_calendar_entries_user_date ON calendar_entries(user_id, created_at);

CREATE TABLE user_settings (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE UNIQUE,
  
  -- Powiadomienia
  daily_reminder_enabled BOOLEAN DEFAULT TRUE,
  reminder_time TIME DEFAULT '09:00:00',
  
  -- Preferencje językowe
  language VARCHAR(10) DEFAULT 'pl',
  
  -- Theme
  theme VARCHAR(20) DEFAULT 'light',  -- 'light', 'dark', 'system'
  
  updated_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE calendar_entries (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE,
  calendar_id UUID REFERENCES mood_calendar(id) ON DELETE CASCADE,
  
  -- Powiązanie z Journey (opcjonalne)
  journey_id VARCHAR(50),
  journey_day INT,
  
  -- Źródło wpisu
  source_type VARCHAR(20),  -- 'journey', 'mood_checkin', 'free_write'
  
  -- Treść odpowiedzi
  content TEXT NOT NULL,  -- plik/odpowiedź użytkownika
  
  -- Emocja przypisana do tego wpisu
  emotion_id VARCHAR(50),  -- np. 'joy', 'sadness'
  emotion_name VARCHAR(50),  -- np. 'Radość'
  color VARCHAR(20),  -- np. '#FFD93D'
  
  created_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE mood_calendar (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE,
  
  entry_date DATE NOT NULL,
  
  -- Główna emocja dnia (do koloru w kalendarzu)
  primary_emotion_id VARCHAR(50) REFERENCES emotions(id),
  primary_emotion_name VARCHAR(50),  -- zapisana nazwa np. "Radość"
  color VARCHAR(20),  -- np. "#FFD93D"
  
  -- Opcjonalne: dodatkowe emocje
  secondary_emotions JSONB,  -- ["joy", "trust"]
  
  created_at TIMESTAMP DEFAULT NOW(),
  
  UNIQUE(user_id, entry_date)
);

CREATE TABLE user_mood_checkins (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE,
  
  -- Wybrany nastrój
  primary_emotion_id VARCHAR(50) REFERENCES emotions(id),
  intensity INT,  -- 1-10 (opcjonalne)
  
  -- Opcjonalne: dodatkowe emocje
  secondary_emotions JSONB,  -- ['joy', 'trust']
  
  -- Odpowiedź na pytanie
  response_text TEXT,
  
  checked_in_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE emotion_questions (
  id SERIAL PRIMARY KEY,
  emotion_id VARCHAR(50) REFERENCES emotions(id),
  question_number INT,  -- 1-3
  question TEXT NOT NULL,
  
  UNIQUE(emotion_id, question_number)
);

CREATE TABLE emotions (
  id VARCHAR(50) PRIMARY KEY,  -- np. 'joy', 'trust', 'fear'
  name VARCHAR(50) NOT NULL,
  name_en VARCHAR(50) NOT NULL,
  description TEXT,
  spectrum VARCHAR(20),  -- 'primary' lub 'subspectrum'
  parent_id VARCHAR(50),  -- dla podspektrów
  wheel_position DECIMAL(5,2),  -- pozycja na kole (0-360)
  is_active BOOLEAN DEFAULT TRUE
);

CREATE TABLE user_responses (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE,
  
  -- Powiązanie do Journey (opcjonalne)
  journey_id VARCHAR(50) REFERENCES journeys(id),
  journey_day INT,  -- 1-7
  
  -- Lub powiązanie do Mood (opcjonalne)
  mood_id VARCHAR(50),
  
  question TEXT NOT NULL,
  response TEXT NOT NULL,  -- Odpowiedź użytkownika
  
  created_at TIMESTAMP DEFAULT NOW(),
  
  -- Metadata
  response_length INT,  -- Liczba znaków
  time_spent_seconds INT  -- Czas od wyświetlenia pytania
);

CREATE TABLE user_journey_progress (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE,
  journey_id VARCHAR(50) REFERENCES journeys(id),
  
  current_day INT DEFAULT 1,  -- 1-7
  started_at TIMESTAMP DEFAULT NOW(),
  completed_at TIMESTAMP,
  
  -- Status: 'in_progress', 'completed', 'paused'
  status VARCHAR(20) DEFAULT 'in_progress',
  
  UNIQUE(user_id, journey_id)
);

CREATE TABLE journey_days (
  id SERIAL PRIMARY KEY,
  journey_id VARCHAR(50) REFERENCES journeys(id),
  day_number INT NOT NULL,  -- 1-7
  day_name VARCHAR(20),  -- 'Poniedziałek', etc.
  question TEXT NOT NULL,
  question_en TEXT NOT NULL,
  
  UNIQUE(journey_id, day_number)
);

CREATE TABLE journeys (
  id VARCHAR(50) PRIMARY KEY,  -- np. 'wewnetrzne-dziecko', 'granice'
  title VARCHAR(100) NOT NULL,
  title_en VARCHAR(100) NOT NULL,
  subtitle TEXT,
  subtitle_en TEXT,
  tone VARCHAR(100),
  tone_en VARCHAR(100),
  is_active BOOLEAN DEFAULT TRUE,
  display_order INT DEFAULT 0
);

CREATE OR REPLACE FUNCTION public.handle_new_user()
RETURNS TRIGGER AS $$
BEGIN
  INSERT INTO public.profiles (id, email)
  VALUES (NEW.id, NEW.email);
  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

CREATE TRIGGER on_auth_user_created
AFTER INSERT ON auth.users
FOR EACH ROW EXECUTE FUNCTION public.handle_new_user();

CREATE TABLE profiles (
  id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  email VARCHAR(255),
  display_name VARCHAR(100),
  timezone VARCHAR(50) DEFAULT 'Europe/Warsaw',
  notification_time TIME DEFAULT '09:00:00',
  onboarding_completed BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
